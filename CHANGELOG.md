# Changelog

> [English](CHANGELOG.en.md) · [中文](CHANGELOG.md)

本文件记录面向用户的版本更新。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.8.3] - 2026-09-07

### Fixed

- 托盘「暂停/继续/取消」仍未响应（0.8.2 修复不彻底）：经核对 tauri-runtime-wry 源码，`run_on_main_thread` 在主线程上是**就地同步执行**的、并不排队，因此 0.8.2 在菜单回调里仍然同步重建了托盘。现在重建改为先落到独立工作线程、再经事件循环真正排队，确保在菜单事件处理完全返回之后才执行。

## [0.8.2] - 2026-09-07

### Fixed

- 点击托盘「暂停/继续/取消」导致应用未响应：托盘菜单回调里同步销毁并重建托盘图标，与正在显示的菜单内部状态互锁死锁主线程。现改为把重建排队到菜单事件处理返回之后执行。
- 匹配预览状态更准确：命中且未登记的文件此前一律显示「待改名登记」，即使文件名已符合当前命名模板（同步时其实会直接登记、免改名下载）。现在区分显示「待改名登记」与「待登记（文件名已符合模板）」，判定逻辑与同步引擎的改名判定一致。
- 界面里的暂停/继续操作现在也会同步刷新托盘菜单标签，不再滞留旧状态。

## [0.8.1] - 2026-09-06

### Added

- 云盘列表缓存落盘：重启应用后云盘页秒开上次的列表，后台自动拉取最新并原位更新；换账号/重新登录后旧缓存自动作废，上传任务结束后自动清理。

### Fixed

- 托盘菜单会随任务开始/结束实时刷新：此前托盘只在启动和切换语言时重建，任务跑起来后菜单不更新，暂停/继续/取消形同虚设；同时补上云盘任务支持——此前托盘只能控制歌单同步，云盘上传/下载进行中无法从托盘暂停或取消。

### Changed

- 发版 CI 新增中英文文案键一致性校验步骤，文案键不同步会直接构建失败。

## [0.8.0] - 2026-09-06

### Added

- **音乐云盘**：侧边栏新增「音乐云盘」页，展示云盘歌曲列表（总数/已用空间统计、搜索过滤、刷新），列表带 120 秒缓存。
- **同步到云盘**：扫描音乐根目录，把云盘中没有的歌上传到云盘。走客户端直传（token → 对象存储 → complete），不受 API 代理服务器请求体限制，FLAC 等大文件可用；内容已在网易服务器的自动秒传；未配置音乐根目录会跳转设置页。
- **手动上传**：云盘页可直接选择音频文件（多选）或整个目录上传到云盘，目录递归扫描音频。
- **多选下载**：云盘列表支持多选（翻页保留选择），一键下载到指定目录（可选目录，默认音乐根目录；已存在的自动跳过）。
- **任务并行**：云盘任务（上传/下载）与歌单同步相互独立、可同时运行，各自拥有进度、暂停/继续/取消，侧栏运行状态合并显示。
- **任务详情**：同步日志每条记录可点开查看该轮任务的明细——云盘任务含逐首对比（已在云盘/重复/未识别）与上传/下载结果，歌单任务含下载/隔离/补录/移除记录；失败行显示具体原因，删除行可直接恢复。支持按文件名与状态/操作筛选、按列三态排序（递增→递减→默认），任务进行中实时刷新。
- 顶栏头像/昵称可点击，回到账号登录页。

### Changed

- 一次同步任务只保留**一条**同步日志：开始时为"进行中"，结束原地更新为 成功/失败/已取消，失败/取消在日志正文中写明原因；任务开始与结束时日志列表即时刷新。
- 原「变更记录」「可恢复的删除记录」两个列表并入同步日志的任务详情。
- 同步日志支持按起止日期时间筛选；搜索匹配翻译后的任务名（如搜"云盘"可命中云盘任务）。
- 匹配预览不再自动开始匹配：打开后选择歌单或点击「开始匹配」才执行。
- 设置页左右两栏顶部对齐，空白留在右下角。
- 界面错误提示全面中文化：后端错误码与翻译键统一，HTTP/网络类错误也不再显示英文原码。

### Fixed

- 歌单同步取消后，同步日志不再残留"进行中"状态。
- 未登记到同步设置的歌单，任务名不再显示为 `#<歌单id>` 占位。
- 云盘列表缓存此前未生效（每次打开都重新请求全部分页）。

## [0.7.2] - 2026-09-06

### Fixed

- 顶部「发现新版本」的去下载链接多了一个 `v`（`.../tag/vv0.7.1` 打不开）：新版本号现以纯版本号返回，展示与链接各自只拼一次 `v`。

## [0.7.1] - 2026-09-06

### Changed

- 移除歌单页「备份到本地目录」按钮（及对应后端逻辑）。
- NCM 转换输出文件名跟随源 .ncm 文件名（不再用 NCM 内嵌歌名），转出的 mp3/flac 与原文件同名、仅换扩展名。
- 应用启动时窗口默认在屏幕居中显示。
- 账号页「刷新统计」按钮：点击时按钮转圈并在统计卡片上显示遮罩反馈，完成/失败有明确提示。
- 设置页「网易云 API 地址」说明中的 NeteaseCloudMusicApiEnhanced 现在可以点击并调用系统默认浏览器打开（其 GitHub 仓库页）；同步修复顶部「发现新版本」的去下载按钮跳转。

### Fixed

- NCM 转换失败：`李荣浩 - 年少有为.ncm` 等文件此前报 "invalid NCM AES padding"——元数据解密使用了错误密钥，音频解流算法也与网易实现不一致；已按官方实现修正，转换结果与官方 ncmdump 一致。
- NCM 转换工具弹窗：进行中不再显示虚假的 0% 进度条（改 spinner）；失败明细不再只显示前 5 条。
- 同步/转换结束后「当前任务」卡片不再残留上一次任务的旧进度（显示 0% 卡住）。

## [0.7.0] - 2026-09-05

### Added

- 本地匹配预览：歌单页新增「匹配预览」入口，可按歌单查看本地音频与网易曲目的匹配结果；支持解析备注中的官方 `163 key(Don't modify)`（AES 解密出网易歌曲 id，与歌单曲目精确匹配），其次用 ID3 标签的标题+艺术家匹配。匹配方式标注 旁车 / 163 key / netease-id / 标签 / 未识别。
- 新下载的歌曲以官方格式写入 163 key 备注（enc=0 Latin1 + lang=XXX，与网易客户端下载的文件字节级一致），右键属性可正常显示；网易 id 不再以纯文本写入标签。
- 顶部栏新增语言 / 主题快捷切换（任意页面可用，即时生效并保存）。
- 本地歌曲改名复用：同步时若歌单文件夹已有同一首歌（经旁车/163 key/标签识别），会登记为已同步并重命名到当前模板，避免重复下载。

### Changed

- 数据时效与性能：网易元数据（歌单列表/曲目/歌词/账号资料）增加进程内 TTL 缓存，UI 重复读取不再重复请求；同步引擎始终使用最新数据不受缓存影响；刷新按钮与匹配预览可强制穿透缓存拉取最新。
- 同步并发性能：worker 共享整份歌单/配置，消除逐曲目大对象克隆。
- 暗色主题柔和化：背景不再纯黑、边框降对比；登录页/设置页内容自适应铺满，设置项改双列布局。
- 界面主题跟随系统/手动切换即时生效（先保存后应用，修复原有时序问题）。

### Fixed

- 歌单列表缓存跨账号串数据（登出/切换账号后仍显示上一账号歌单）——缓存现按账号隔离。
- 下载进度不再触发整棵界面重渲染（进度独立订阅）；空闲时停止无用的每秒轮询。
- 匹配预览弹窗被歌曲列表抽屉遮挡（z-index）。

## [0.6.0] - 2026-09-05

### Added

- 下载模式：每个歌单（或全局）可选择 镜像 / 仅新增 / 仅删除，作用于「歌单 → 本地」下载侧（默认镜像）。
- 可选补录：设置（全局或按歌单）开启“把手动放入的歌补进网易歌单”后，同步会把歌单本地文件夹中不在网易歌单的音频（优先读 .netease.json 标记，否则用标题/歌手/专辑/时长/md5 调 /search/match 匹配）加入网易歌单；仅对“我创建的”歌单生效，且只新增、绝不反向删歌（避免本地手动删歌导致歌单被连带删除）。默认关闭。
- 完整的变更记录：每次同步的每条新增/删除（下载缺失、本地多余隔离、加入歌单、从歌单移除）都写入变更流水；同步任务页可查看并按曲目恢复。
- 删除日志与恢复：本地被隔离的文件可还原回原路径；网易歌单被移出的曲目可加回；每次同步自动保存歌单歌曲快照，支持一键把歌单恢复到任一历史状态（仅限我创建的歌单）。
- 歌单分组：歌单列表可按「全部 / 我创建的 / 我收藏的」分段筛选（依据 creator.userId）；每歌单可单独覆盖方向与模式。
- 账号统计面板：登录后展示头像、网易等级、VIP、关注/粉丝、创建/收藏歌单数、喜欢数，以及本地累计同步统计（次数/新增/隔离/NCM 转换/失败、当前本地文件数）。
- 独立 NCM 转换工具：设置页内可打开弹窗，选择 .ncm 文件或目录批量转换，可保留源文件或删除、可忽略已有转换标记重转。
- 顶栏显示登录头像。

### Changed

- 手动同步、启动/定时/托盘同步统一按各歌单的方向×模式配置执行。

## [0.5.0] - 2026-09-05

### Added

- 同步前音质/版权预检：下载前先批量读取歌曲详情（`/song/detail` privilege），歌曲列表标注当前账号可下载性与最高音质，无权限/灰歌/需购买/地区限制在下载前即提示。
- 并发下载：按设置中的最大并发数并行下载歌曲（信号量限流），并预取直链减少逐首请求；每首歌失败自动重试（可配置次数，指数退避）。
- 失败不再中断批量：单曲下载循环遇到失败会收集成列表，弹窗内可“仅重试失败的”；同步结果可展开查看每首失败曲目与原因。
- 手机短信验证码登录（二维码之外的备选登录方式）。
- “备份到本地目录”：一键把「我喜欢的音乐」或「已购单曲」下载到所选目录（不计入歌单同步状态）。
- 歌单详情页新增“清理不在歌单的文件”：把本地已有但已不在歌单里的文件移入隔离区。
- 歌曲行显示本地文件状态：已同步 / 文件丢失 / 大小，并可“在文件夹中显示”。
- 启动后静默检查 GitHub Releases，有新版本时在顶部提示。
- 设置新增：请求 User-Agent、是否启用下载前预检、单曲重试次数。

### Changed

- 歌曲详情接口（`/song/detail`）在个别 API 实例不支持时静默降级，不影响同步。

## [0.4.0] - 2026-09-04

### Fixed

- 修复会员歌曲只能下载试听片段：所有请求现在以 `cookie` 参数携带会话（Enhanced 认证通道），并按账号权益返回完整音质。
- 修复扫码登录后始终“登录状态未确认”：移除 `/login/status` 重复的 `cookie` 参数导致服务端解析冲突。
- 修复错误提示显示原始 JSON：前端正确解析并翻译后端错误码。
- 修复 88VIP 下载会员歌返回 705KB/45 秒试听：地址接口不再注入 `randomCNIP`，保持会员判定。
- 单曲下载到非音乐根目录的自定义目录时，不再标记为“已同步”。

### Added

- 逐首歌的下载日志 `logs/track-downloads.jsonl`（成功/跳过/失败/字节数/音质/错误）。
- 通用日志 `logs/app.log.jsonl`，记录同步开始/结束/失败与命令错误；日志自动轮转并按容量清理。
- 设置中可切换下载地址接口来源：自动（song/url/v1 优先）或优先 song/download/url 系。
- 应用启动默认进入账号登录页；已登录自动跳到歌单页。
- 歌单列表 60 秒缓存，避免页面切换卡顿；手动刷新或操作后强制刷新。

### Changed

- “使用随机中国 IP”默认改为关闭，避免影响会员音质判定。

## [0.3.0] - 2026-09-02

### Added

- 界面国际化：支持中文 / English，可在设置中即时切换，无需重启；托盘与窗口标题同步切换。
- 可配置的歌手分隔符，默认 `、`，文件名与 ID3 标签保持一致。
- 单曲下载支持自定义保存目录、文件名模板、音质、是否下载歌词与覆盖已存在文件。
- 命令行模式 `--cli`：`status`、`sync <id|all>`、`download <playlist> <track>`，JSON 输出并支持 `--output` 写文件。
- 关闭窗口默认缩小到系统托盘，可在设置中关闭该行为。
- 音质不可用时自动降级（例：无损 → 极高 → 较高 → 标准）。

### Fixed

- 修复未设置音乐根目录时，单曲下载即使已选择保存目录仍报错的问题。
- 修复下载弹窗文件名输入框提示语显示不全的问题。
- 修复单曲下载未勾选歌词却仍下载歌词的问题。
- 修复切换语言后出现多个托盘图标的问题。
- 歌单“覆盖已存在文件”开关移入歌单列表每条记录，便于快捷设置。

## [0.2.0] - 2026-09-02

### Added

- 歌单详情与歌曲列表：可查看歌曲状态，支持单曲下载并选择保存目录、文件名模板、音质、是否下载歌词与覆盖已存在文件。
- 歌单卡片显示“已同步 x/y”进度与最近一次同步时间、结果。
- 歌单级“覆盖已存在文件”开关：开启后同步强制重新下载并覆盖。
- 同步时识别本地已存在文件并登记为已同步，不再重复下载或覆盖用户文件。
- 设置自动保存，修改任意设置即自动生效；选择音乐根目录后立即保存。
- NCM 转换后可选择保留或删除原始 `.ncm` 文件。

### Fixed

- 修复修改设置后自动退出登录：自动保存不再清空登录凭据与歌单配置。
- 修复会话失效（`code:301`）时歌单页与登录页登录状态不一致。
- 修复同步歌曲下载到“未命名”文件夹：歌单名从 `/playlist/detail` 兜底获取。
- 修复同步后仍显示“从未同步”。
- 修复设置页音乐根目录选择按钮与输入框未对齐。

### Changed

- 默认文件名模板改为 `{歌手} - {标题}`。
- README 与开发文档改用 [NeteaseCloudMusicApi Enhanced](https://github.com/NeteaseCloudMusicApiEnhanced/api-enhanced) 作为兼容服务参考。

## [0.1.1] - 2026-09-01

### Fixed

- 修复 Windows 发布版启动时显示终端黑窗的问题。
- 替换为空白/黑色图标为多尺寸音乐同步图标，并用于窗口、任务栏和托盘。
- 修复二维码登录后有效会话 cookie 未及时保存，导致仍显示未登录的问题。
- 修复登录状态校验未通过 Enhanced API 的 `cookie` 参数传递会话 cookie，导致授权后一直显示未登录的问题。

### Added

- 在设置中增加 HTTP(S) 代理地址，用于 API 服务连通性。

## [0.1.0] - 2026-08-31

### Added

- 新增 Windows 桌面音乐同步应用，支持网易云二维码登录与歌单管理。
- 支持手动、启动时和定时同步，缺失歌曲下载及本地多余文件隔离。
- 支持文件夹与文件名模板、LRC、M3U8、基础音频元数据和 NCM 自动转换。
- 提供便携模式、自定义数据目录迁移、同步日志、系统托盘和单实例运行。
- 提供 NSIS 安装包与便携 ZIP 构建流程。

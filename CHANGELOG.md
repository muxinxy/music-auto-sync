# Changelog

> [English](CHANGELOG.en.md) · [中文](CHANGELOG.md)

本文件记录面向用户的版本更新。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.3.0] - 2026-09-02

### Added

- 界面国际化：支持中文 / English，可在设置中即时切换，无需重启；托盘与窗口标题同步切换。
- 可配置的歌手分隔符，默认 `、`，文件名与 ID3 标签保持一致。
- 单曲下载支持自定义保存目录、文件名模板、音质、是否下载歌词与覆盖已存在文件。
- 命令行模式 `--cli`：`status`、`sync <id|all>`、`download <playlist> <track>`，JSON 输出并支持 `--output` 写文件。
- 关闭窗口默认缩小到系统托盘，可在设置中关闭该行为。
- 音质不可用时自动降级（例：无损 → 极高 → 较高 → 标准）。

### Fixed

- 修复未设置音乐根目录时，单曲下载即使已选择保存目录仍报错的问题。
- 修复下载弹窗文件名输入框提示语显示不全的问题。
- 修复单曲下载未勾选歌词却仍下载歌词的问题。
- 修复切换语言后出现多个托盘图标的问题。
- 歌单“覆盖已存在文件”开关移入歌单列表每条记录，便于快捷设置。

## [0.2.0] - 2026-09-02

### Added

- 歌单详情与歌曲列表：可查看歌曲状态，支持单曲下载并选择保存目录、文件名模板、音质、是否下载歌词与覆盖已存在文件。
- 歌单卡片显示“已同步 x/y”进度与最近一次同步时间、结果。
- 歌单级“覆盖已存在文件”开关：开启后同步强制重新下载并覆盖。
- 同步时识别本地已存在文件并登记为已同步，不再重复下载或覆盖用户文件。
- 设置自动保存，修改任意设置即自动生效；选择音乐根目录后立即保存。
- NCM 转换后可选择保留或删除原始 `.ncm` 文件。

### Fixed

- 修复修改设置后自动退出登录：自动保存不再清空登录凭据与歌单配置。
- 修复会话失效（`code:301`）时歌单页与登录页登录状态不一致。
- 修复同步歌曲下载到“未命名”文件夹：歌单名从 `/playlist/detail` 兜底获取。
- 修复同步后仍显示“从未同步”。
- 修复设置页音乐根目录选择按钮与输入框未对齐。

### Changed

- 默认文件名模板改为 `{歌手} - {标题}`。
- README 与开发文档改用 [NeteaseCloudMusicApi Enhanced](https://github.com/NeteaseCloudMusicApiEnhanced/api-enhanced) 作为兼容服务参考。

## [0.1.1] - 2026-09-01

### Fixed

- 修复 Windows 发布版启动时显示终端黑窗的问题。
- 替换为空白/黑色图标为多尺寸音乐同步图标，并用于窗口、任务栏和托盘。
- 修复二维码登录后有效会话 cookie 未及时保存，导致仍显示未登录的问题。
- 修复登录状态校验未通过 Enhanced API 的 `cookie` 参数传递会话 cookie，导致授权后一直显示未登录的问题。

### Added

- 在设置中增加 HTTP(S) 代理地址，用于 API 服务连通性。

## [0.1.0] - 2026-08-31

### Added

- 新增 Windows 桌面音乐同步应用，支持网易云二维码登录与歌单管理。
- 支持手动、启动时和定时同步，缺失歌曲下载及本地多余文件隔离。
- 支持文件夹与文件名模板、LRC、M3U8、基础音频元数据和 NCM 自动转换。
- 提供便携模式、自定义数据目录迁移、同步日志、系统托盘和单实例运行。
- 提供 NSIS 安装包与便携 ZIP 构建流程。
